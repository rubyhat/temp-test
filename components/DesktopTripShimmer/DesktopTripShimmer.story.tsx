import React from 'react';
import { storiesOf } from '@storybook/react';

import { DesktopTripShimmer } from './DesktopTripShimmer';

storiesOf('DesktopTripShimmer', module).add('default', () => (
    <DesktopTripShimmer />
));
