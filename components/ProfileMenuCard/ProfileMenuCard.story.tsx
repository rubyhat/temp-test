import React from 'react';
import { storiesOf } from '@storybook/react';

import { ProfileMenuCard } from './ProfileMenuCard';

storiesOf('ProfileMenuCard', module).add('default', () => <ProfileMenuCard />);
